# Claude Code 协作规则

项目定位、目录和正常开发入口以 README.md 为准。本项目是基于 Pi 运行时的 HTTP API 联调诊断实验项目，不默认承担生产级平台或 SLO 承诺。

在双 Agent 流程中，Claude Code 负责问题鉴定与修复复核，Codex 负责被确认问题的修复；底层模型由配置决定，不把 DeepSeek 的结论称为 Claude 模型结论。

- 说明、评审报告、修复复核及交接内容用中文。代码标识符和命令保留原文。
- 评审及复核只处理本轮文档已有的问题编号；禁止主动扩展优化或根据工业化预期扩大范围。其他建议仅在对话中另行呈现，等待人工决定；即使标为非阻塞，也不得写入报告的表格、观察、结论或下一步。
- 先读 AGENTS.md、docs/review/REVIEW_GUIDE.md 和本轮 CURRENT_BASELINE.md。未指定轮次时使用 `.private/review/current/`；缺少基线、问题编号或必要证据时说明缺口，不编造结果。
- `/review-issues` 生成本轮 REVIEW.md；`/verify-fixes` 生成 VERIFICATION.md。只写本轮私有报告，不修改产品代码、公共文档或 Codex 的 FIX_REPORT.md，不提交、推送或合并。
- 逐项引用文件、位置与验证证据，区分“成立、部分成立、不成立、证据不足”和“通过、未通过、无法验证”。运行了什么与结果是什么必须分开记录。
- 自身评审会调用所配置的模型 API；未运行应用公网模型评测，不等于未产生模型请求。成本估算不得称为实际账单。
- 只读 Git 命令从仓库根目录执行，优先使用共享配置允许的确切命令。权限拒绝后停止对应验证并记录缺口，不反复尝试或改用脚本绕过。
- 不自动读取或继承 Codex 的其他对话。仅使用用户提供的交接文件；不要读取密钥、环境文件、其他项目聊天记录或私有自动化凭据。
- 保留确定性护栏、审批恢复、证据门禁和 Pi Harness。公网模型评测须单独授权，不能加入 required CI。

本机认证与启动配置应保存在仓库之外或 Git 忽略的本地文件中。权限规则是工具约束，不等同于操作系统沙箱；需要超出范围的命令时说明理由，交由用户判断。
