# 项目协作规则

正常开发遵循 README.md、CONTRIBUTING.md 和现有 CI。本文件不要求普通开发任务先进行双 Agent 评审。

- 文档和人工审核输出使用中文。不要改变项目作为 HTTP API 联调诊断实验项目的定位。
- 对评审与验证修复任务，先读取 docs/review/REVIEW_GUIDE.md 和指定轮次的 CURRENT_BASELINE.md；仅处理文档已经提出的问题编号。未指定轮次时使用 `.private/review/current/`。
- Claude Code 负责问题鉴定和修复复核；Codex 根据确认的问题实施最小修复并写 FIX_REPORT.md。原始评审结论不得悄悄改写。
- 不把评审意见直接视为事实。先确认成立、部分成立、不成立或证据不足，再决定修复；产品范围变更和缺少明确需求的设计取舍提交人工判断。
- 新发现的优化仅在对话中另列，不加入当前问题清单、修复或验收条件，除非用户明确纳入。
- 实际基线、报告、实验、对话摘录与本机说明放入 `.private/review/` 等忽略目录。公开模板只使用仓库相对路径和占位符，禁止包含密钥、本机绝对路径及私人对话。
- 修复遵循 CONTRIBUTING.md 的 Issue、分支、PR 和检查流程；保留确定性护栏、审批恢复及证据门禁。模型联网评测保持可选，不能加入 required CI。

当前 CI 的命令以 `.github/workflows/ci.yml` 为准。报告中的历史版本、旧测试数量及旧评测结果不能代替本轮验证。
